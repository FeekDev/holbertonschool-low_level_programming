#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * print_rev - the function print reverse a string
 *@s: the parameter
 *
 *
 * Return: Always 0.
 */
void print_rev(char *s)
{
	while (*s != '\0')
	{
	*s++;
	}
}
